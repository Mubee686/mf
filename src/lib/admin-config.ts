/**
 * Shared, client-safe admin configuration. No secrets live here —
 * just the email address that identifies the admin account, used to:
 *  - gate the Admin link on the dashboard
 *  - address the membership-expiry notification email
 *  - show the contact email on the dashboard
 */
export const ADMIN_EMAIL = "mfsmctrader786@gmail.com";
